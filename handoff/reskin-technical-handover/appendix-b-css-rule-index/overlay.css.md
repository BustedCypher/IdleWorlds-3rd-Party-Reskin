# overlay.css — rule index

> Generated by `tools/css-inventory.mjs` from `src/styles/overlay.css` at commit `059081671bf3`. Do not edit by hand.

Injected as part of `<style data-iw-style="overlay">` — stylesheet 6 of 7 in injection order (src/content.js injectPresentationStyles()).

56 lines · 3,019 bytes · 2 rules · 2 selectors · 11 declarations · 11 `!important` · 0 `@media` · 0 `@container` · 0 `@keyframes` · 0 `@font-face`

**Columns.** *Line* is the rule's first line in the source file at the commit above. *Context* is the enclosing `@media`/`@container`/`@supports` chain. *Specificity* is the highest (id, class, type) of the rule's selector list, computed per Selectors Level 4 (`:is()/:not()/:has()` take their most specific argument, `:where()` counts zero). *Decl* is the declaration count; *!imp* the number marked `!important`. *Properties* lists every property the rule sets, with `!` marking `!important`. *Hooks* separates skin-owned hooks (`data-iw-*`, `data-fs-*`, `.fs-*`, `.iw-*`, `#iw-tip`) from game/host hooks (classes, attributes, elements the extension does not own). *Fragility* flags selectors that depend on utility class names, attribute substrings, element position or sibling order. *Comment* is the nearest authored comment for the rule's block (its own when marked ●, otherwise the last one above it in the same block, marked ○).

## Rules

| # | Line | Context | Selector(s) | Specificity | Decl / !imp | Properties | Hooks | Fragility | Comment |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | 21 | — | `[data-iw-overlay="scrim"]` | (0,1,0) | 3 / 3 | background!, backdrop-filter!, -webkit-backdrop-filter! | skin: `[data-iw-overlay="scrim"]` | — | ● L1: overlay.css — generic pop-up / modal chrome OverlayFramer.js tags the game's floating dialogs from structure (a viewport-covering `position: fixed` layer that reads as a backdrop,… |
| 2 | 30 | — | `[data-iw-overlay="panel"]` | (0,1,0) | 8 / 8 | isolation!, border!, border-image!, border-radius!, background!, background-blend-mode!, background-attachment!, box-shadow! | skin: `[data-iw-overlay="panel"]` | — | ● L22: The game already ships a dark translucent fill + blur here; deepen it a touch so it matches the skin's other smoked-glass chrome. |
