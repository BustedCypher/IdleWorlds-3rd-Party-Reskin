# skillcard-v2-runtime-safe.css — rule index

> Generated by `tools/css-inventory.mjs` from `src/styles/skillcard-v2-runtime-safe.css` at commit `059081671bf3`. Do not edit by hand.

Injected as part of `<style data-iw-style="skillpanel">` — stylesheet 4 of 7 in injection order, part 3 of 5 in that element's concatenation (src/content.js injectPresentationStyles()).

30 lines · 1,311 bytes · 1 rules · 1 selectors · 14 declarations · 14 `!important` · 0 `@media` · 0 `@container` · 0 `@keyframes` · 0 `@font-face`

**Columns.** *Line* is the rule's first line in the source file at the commit above. *Context* is the enclosing `@media`/`@container`/`@supports` chain. *Specificity* is the highest (id, class, type) of the rule's selector list, computed per Selectors Level 4 (`:is()/:not()/:has()` take their most specific argument, `:where()` counts zero). *Decl* is the declaration count; *!imp* the number marked `!important`. *Properties* lists every property the rule sets, with `!` marking `!important`. *Hooks* separates skin-owned hooks (`data-iw-*`, `data-fs-*`, `.fs-*`, `.iw-*`, `#iw-tip`) from game/host hooks (classes, attributes, elements the extension does not own). *Fragility* flags selectors that depend on utility class names, attribute substrings, element position or sibling order. *Comment* is the nearest authored comment for the rule's block (its own when marked ●, otherwise the last one above it in the same block, marked ○).

## Rules

| # | Line | Context | Selector(s) | Specificity | Decl / !imp | Properties | Hooks | Fragility | Comment |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | 14 | — | `html[data-iw-skill-card-design="new"] .compact-panel.fs-skill-panel[data-iw-skill-v2="1"] [data-iw-skill-v2-section]` | (0,5,1) | 14 / 14 | display!, position!, width!, min-width!, max-width!, height!, min-height!, max-height!, margin!, padding!, overflow!, clip-path!, opacity!, pointer-events! | skin: `.fs-skill-panel` `[data-iw-skill-card-design="new"]` `[data-iw-skill-v2="1"]` `[data-iw-skill-v2-section]`<br>host: `.compact-panel` | — | ● L1: V2 may visually tuck native semantic rows away, but SkillPanelRenderer also uses computed display/visibility to choose the live reward/action among stale React siblings. |
