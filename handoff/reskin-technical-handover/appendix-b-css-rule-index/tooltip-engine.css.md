# tooltip-engine.css — rule index

> Generated by `tools/css-inventory.mjs` from `src/styles/tooltip-engine.css` at commit `059081671bf3`. Do not edit by hand.

Injected as part of `<style data-iw-style="tooltip-engine">` — stylesheet 2 of 7 in injection order (src/content.js injectPresentationStyles()).

412 lines · 12,568 bytes · 51 rules · 54 selectors · 260 declarations · 24 `!important` · 1 `@media` · 0 `@container` · 0 `@keyframes` · 0 `@font-face`

**Columns.** *Line* is the rule's first line in the source file at the commit above. *Context* is the enclosing `@media`/`@container`/`@supports` chain. *Specificity* is the highest (id, class, type) of the rule's selector list, computed per Selectors Level 4 (`:is()/:not()/:has()` take their most specific argument, `:where()` counts zero). *Decl* is the declaration count; *!imp* the number marked `!important`. *Properties* lists every property the rule sets, with `!` marking `!important`. *Hooks* separates skin-owned hooks (`data-iw-*`, `data-fs-*`, `.fs-*`, `.iw-*`, `#iw-tip`) from game/host hooks (classes, attributes, elements the extension does not own). *Fragility* flags selectors that depend on utility class names, attribute substrings, element position or sibling order. *Comment* is the nearest authored comment for the rule's block (its own when marked ●, otherwise the last one above it in the same block, marked ○).

## Rules

| # | Line | Context | Selector(s) | Specificity | Decl / !imp | Properties | Hooks | Fragility | Comment |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | 27 | — | `.iw-item-ref` | (0,1,0) | 18 / 12 | display, background!, border!, border-bottom!, border-radius!, padding!, margin!, box-shadow!, font!, line-height!, font-weight!, letter-spacing!, text-transform!, color, cursor, text-decoration, transition, vertical-align | skin: `.iw-item-ref` | — | ● L1: Tooltip engine — rich item-card treatment The card is a FRAMED SURFACE, and it is framed the way every other framed surface in the skin is (see ui-system.css, "Shared forged frame… |
| 2 | 47 | — | `.iw-item-ref:hover`<br>`.iw-item-ref:focus-visible` | max (0,2,0) | 2 / 1 | color, border-bottom-color! | skin: `.iw-item-ref` | — | ○ L1: Tooltip engine — rich item-card treatment The card is a FRAMED SURFACE, and it is framed the way every other framed surface in the skin is (see ui-system.css, "Shared forged frame… |
| 3 | 52 | — | `.iw-item-ref:focus-visible` | (0,2,0) | 2 / 2 | outline!, outline-offset! | skin: `.iw-item-ref` | — | ○ L1: Tooltip engine — rich item-card treatment The card is a FRAMED SURFACE, and it is framed the way every other framed surface in the skin is (see ui-system.css, "Shared forged frame… |
| 4 | 56 | — | `.iw-item-info` | (0,1,0) | 1 / 0 | display | skin: `.iw-item-info` | — | ○ L1: Tooltip engine — rich item-card treatment The card is a FRAMED SURFACE, and it is framed the way every other framed surface in the skin is (see ui-system.css, "Shared forged frame… |
| 5 | 58 | — | `.iw-tip` | (0,1,0) | 27 / 0 | --fs-tip-w, --fs-tip-pad-x, --fs-tip-art, --fs-tip-corner-h, --fs-tip-corner-w, position, z-index, width, max-width, box-sizing, background, background-blend-mode, border, border-radius, box-shadow, max-height, max-height, flex-direction, overflow, overscroll-behavior, font-family, font-size, line-height, color, opacity, pointer-events, transition | skin: `.iw-tip` | — | ○ L1: Tooltip engine — rich item-card treatment The card is a FRAMED SURFACE, and it is framed the way every other framed surface in the skin is (see ui-system.css, "Shared forged frame… |
| 6 | 99 | — | `.iw-tip::before` | (0,1,1) | 10 / 0 | content, position, z-index, left, right, top, height, background, opacity, pointer-events | skin: `.iw-tip` | — | ● L98: Gold hairline across the head of the frame — the shared frame's ::before. |
| 7 | 115 | — | `.iw-tip::after` | (0,1,1) | 9 / 0 | content, position, inset, z-index, pointer-events, border-style, border-width, border-color, border-image | skin: `.iw-tip` | — | ● L111: Four mirrored corners from one 176x190 sheet — the same asset, slice and `stretch` the inventory panel, quest cards, skill actions, the activity panels and the header all draw. |
| 8 | 127 | — | `.iw-tip &gt; *` | (0,1,0) | 2 / 0 | position, z-index | skin: `.iw-tip` | — | ● L126: Card content sits above the ornament. |
| 9 | 129 | — | `.iw-tip.is-open` | (0,2,0) | 2 / 0 | opacity, pointer-events | skin: `.iw-tip`<br>host: `.is-open` | — | ○ L126: Card content sits above the ornament. |
| 10 | 130 | — | `.iw-tip:focus-visible` | (0,2,0) | 2 / 0 | outline, outline-offset | skin: `.iw-tip` | — | ○ L126: Card content sits above the ornament. |
| 11 | 143 | — | `.iw-tip-head` | (0,1,0) | 9 / 0 | position, flex, display, align-items, gap, padding, box-sizing, background, border-bottom | skin: `.iw-tip-head` | — | ● L135: ── Head: sprite slot, name, badges The art used to be a 100px box absolutely positioned in the top-right, which forced `min-height: 116px` on the head and reserved 118px of paddin… |
| 12 | 154 | — | `.iw-tip-title-block` | (0,1,0) | 2 / 0 | flex, min-width | skin: `.iw-tip-title-block` | — | ○ L135: ── Head: sprite slot, name, badges The art used to be a 100px box absolutely positioned in the top-right, which forced `min-height: 116px` on the head and reserved 118px of paddin… |
| 13 | 155 | — | `.iw-tip-name` | (0,1,0) | 7 / 0 | font-family, font-size, font-weight, line-height, letter-spacing, word-break, text-shadow | skin: `.iw-tip-name` | — | ○ L135: ── Head: sprite slot, name, badges The art used to be a 100px box absolutely positioned in the top-right, which forced `min-height: 116px` on the head and reserved 118px of paddin… |
| 14 | 168 | — | `.iw-tip-art` | (0,1,0) | 13 / 0 | flex, width, height, box-sizing, display, place-items, padding, background-color, border, border-radius, box-shadow, overflow, pointer-events | skin: `.iw-tip-art` | — | ● L165: The item slot, built like .fs-inv-icon: black ground, brass edge, 2px radius, and overflow visible so the AtlasService enhancement badge can overhang. |
| 15 | 186 | — | `.iw-tip-art-host`<br>`.iw-tip-art-fallback` | max (0,1,0) | 10 / 0 | --fs-tip-art-inner, position, display, width, height, min-width, min-height, max-width, max-height, background-repeat | skin: `.iw-tip-art-host` `.iw-tip-art-fallback` | — | ○ L165: The item slot, built like .fs-inv-icon: black ground, brass edge, 2px radius, and overflow visible so the AtlasService enhancement badge can overhang. |
| 16 | 200 | — | `.iw-tip-art-fallback` | (0,1,0) | 4 / 0 | display, place-items, font-size, opacity | skin: `.iw-tip-art-fallback` | — | ● L188: 8px padding + 1px border on each side. |
| 17 | 206 | — | `.iw-tip-close` | (0,1,0) | 12 / 6 | flex, width, height, display, place-items, padding!, border!, border-radius!, background-color!, color!, font!, cursor | skin: `.iw-tip-close` | — | ○ L188: 8px padding + 1px border on each side. |
| 18 | 220 | — | `.iw-tip-close:hover`<br>`.iw-tip-close:focus-visible` | max (0,2,0) | 3 / 3 | border-color!, color!, outline! | skin: `.iw-tip-close` | — | ○ L188: 8px padding + 1px border on each side. |
| 19 | 228 | — | `.iw-tip-badges` | (0,1,0) | 5 / 0 | display, flex-wrap, align-items, gap, margin-top | skin: `.iw-tip-badges` | — | ● L227: Chips, not pills: round pills are not the house style (base.css). |
| 20 | 235 | — | `.iw-tip-badge` | (0,1,0) | 10 / 0 | font-size, font-weight, line-height, letter-spacing, white-space, padding, border, border-radius, background, color | skin: `.iw-tip-badge` | — | ○ L227: Chips, not pills: round pills are not the house style (base.css). |
| 21 | 247 | — | `.iw-tip-badge.t` | (0,2,0) | 2 / 0 | color, border-color | skin: `.iw-tip-badge`<br>host: `.t` | — | ○ L227: Chips, not pills: round pills are not the house style (base.css). |
| 22 | 251 | — | `.iw-tip-badge.req` | (0,2,0) | 2 / 0 | color, border-color | skin: `.iw-tip-badge`<br>host: `.req` | — | ○ L227: Chips, not pills: round pills are not the house style (base.css). |
| 23 | 256 | — | `.iw-tip-body` | (0,1,0) | 7 / 0 | flex, min-height, padding, overflow-y, overscroll-behavior, -webkit-overflow-scrolling, scrollbar-gutter | skin: `.iw-tip-body` | — | ○ L227: Chips, not pills: round pills are not the house style (base.css). |
| 24 | 265 | — | `.iw-tip-sec` | (0,1,0) | 3 / 0 | margin-top, padding-top, border-top | skin: `.iw-tip-sec` | — | ○ L227: Chips, not pills: round pills are not the house style (base.css). |
| 25 | 270 | — | `.iw-tip-sec:first-child` | (0,2,0) | 3 / 0 | margin-top, padding-top, border-top | skin: `.iw-tip-sec` | position-dependent :first-child | ○ L227: Chips, not pills: round pills are not the house style (base.css). |
| 26 | 273 | — | `.iw-tip-sec-title` | (0,1,0) | 7 / 0 | font-family, font-size, font-weight, letter-spacing, text-transform, color, margin-bottom | skin: `.iw-tip-sec-title` | — | ● L271: The house eyebrow: Cinzel, tracked-out caps, dim gold — the treatment the inventory pager and the skill kickers already use. |
| 27 | 282 | — | `.iw-tip-effect` | (0,1,0) | 3 / 0 | color, font-size, line-height | skin: `.iw-tip-effect` | — | ○ L271: The house eyebrow: Cinzel, tracked-out caps, dim gold — the treatment the inventory pager and the skill kickers already use. |
| 28 | 288 | — | `.iw-tip-stats` | (0,1,0) | 3 / 0 | display, grid-template-columns, gap | skin: `.iw-tip-stats` | — | ○ L271: The house eyebrow: Cinzel, tracked-out caps, dim gold — the treatment the inventory pager and the skill kickers already use. |
| 29 | 298 | — | `.iw-tip-stats:not(:has(&gt; :nth-child(2)))` | (0,2,0) | 1 / 0 | grid-template-columns | skin: `.iw-tip-stats` | position-dependent :nth-child | ● L293: A single stat block in a two-column grid strands its value in the middle of the card with an empty column beside it — the "Base value 551g" case, which is most trade goods and eve… |
| 30 | 301 | — | `.iw-tip-stat-block` | (0,1,0) | 1 / 0 | min-width | skin: `.iw-tip-stat-block` | — | ○ L293: A single stat block in a two-column grid strands its value in the middle of the card with an empty column beside it — the "Base value 551g" case, which is most trade goods and eve… |
| 31 | 302 | — | `.iw-tip-stat` | (0,1,0) | 7 / 0 | min-width, display, justify-content, align-items, gap, font-size, padding | skin: `.iw-tip-stat` | — | ○ L293: A single stat block in a two-column grid strands its value in the middle of the card with an empty column beside it — the "Base value 551g" case, which is most trade goods and eve… |
| 32 | 311 | — | `.iw-tip-stat .k` | (0,2,0) | 3 / 0 | min-width, color, white-space | skin: `.iw-tip-stat`<br>host: `.k` | — | ○ L293: A single stat block in a two-column grid strands its value in the middle of the card with an empty column beside it — the "Base value 551g" case, which is most trade goods and eve… |
| 33 | 316 | — | `.iw-tip-stat .v` | (0,2,0) | 7 / 0 | flex, font-family, font-size, color, font-weight, text-align, font-variant-numeric | skin: `.iw-tip-stat`<br>host: `.v` | — | ○ L293: A single stat block in a two-column grid strands its value in the middle of the card with an empty column beside it — the "Base value 551g" case, which is most trade goods and eve… |
| 34 | 325 | — | `.iw-tip-stat .v.amber` | (0,3,0) | 1 / 0 | color | skin: `.iw-tip-stat`<br>host: `.v` `.amber` | — | ○ L293: A single stat block in a two-column grid strands its value in the middle of the card with an empty column beside it — the "Base value 551g" case, which is most trade goods and eve… |
| 35 | 326 | — | `.iw-tip-stat .v.good` | (0,3,0) | 1 / 0 | color | skin: `.iw-tip-stat`<br>host: `.v` `.good` | — | ○ L293: A single stat block in a two-column grid strands its value in the middle of the card with an empty column beside it — the "Base value 551g" case, which is most trade goods and eve… |
| 36 | 327 | — | `.iw-tip-stat-note` | (0,1,0) | 4 / 0 | margin-top, color, font-size, line-height | skin: `.iw-tip-stat-note` | — | ○ L293: A single stat block in a two-column grid strands its value in the middle of the card with an empty column beside it — the "Base value 551g" case, which is most trade goods and eve… |
| 37 | 334 | — | `.iw-tip-acq` | (0,1,0) | 2 / 0 | position, padding | skin: `.iw-tip-acq` | — | ○ L293: A single stat block in a two-column grid strands its value in the middle of the card with an empty column beside it — the "Base value 551g" case, which is most trade goods and eve… |
| 38 | 338 | — | `.iw-tip-acq::before` | (0,1,1) | 7 / 0 | content, position, left, top, bottom, width, background | skin: `.iw-tip-acq` | — | ○ L293: A single stat block in a two-column grid strands its value in the middle of the card with an empty column beside it — the "Base value 551g" case, which is most trade goods and eve… |
| 39 | 347 | — | `.iw-tip-acq-main` | (0,1,0) | 3 / 0 | font-size, font-weight, color | skin: `.iw-tip-acq-main` | — | ○ L293: A single stat block in a two-column grid strands its value in the middle of the card with an empty column beside it — the "Base value 551g" case, which is most trade goods and eve… |
| 40 | 352 | — | `.iw-tip-acq-sub` | (0,1,0) | 4 / 0 | font-size, color, margin-top, line-height | skin: `.iw-tip-acq-sub` | — | ○ L293: A single stat block in a two-column grid strands its value in the middle of the card with an empty column beside it — the "Base value 551g" case, which is most trade goods and eve… |
| 41 | 358 | — | `.iw-tip-acq.is-unknown .iw-tip-acq-main` | (0,3,0) | 2 / 0 | color, font-style | skin: `.iw-tip-acq` `.iw-tip-acq-main`<br>host: `.is-unknown` | — | ○ L293: A single stat block in a two-column grid strands its value in the middle of the card with an empty column beside it — the "Base value 551g" case, which is most trade goods and eve… |
| 42 | 359 | — | `.iw-tip-flavour` | (0,1,0) | 5 / 0 | font-family, font-style, font-size, color, line-height | skin: `.iw-tip-flavour` | — | ○ L293: A single stat block in a two-column grid strands its value in the middle of the card with an empty column beside it — the "Base value 551g" case, which is most trade goods and eve… |
| 43 | 367 | — | `.iw-tip-foot` | (0,1,0) | 9 / 0 | flex, min-height, padding, border-top, background, display, align-items, justify-content, gap | skin: `.iw-tip-foot` | — | ○ L293: A single stat block in a two-column grid strands its value in the middle of the card with an empty column beside it — the "Base value 551g" case, which is most trade goods and eve… |
| 44 | 378 | — | `.iw-tip-foot:empty` | (0,2,0) | 1 / 0 | display | skin: `.iw-tip-foot` | — | ○ L293: A single stat block in a two-column grid strands its value in the middle of the card with an empty column beside it — the "Base value 551g" case, which is most trade goods and eve… |
| 45 | 379 | — | `.iw-tip-link` | (0,1,0) | 5 / 0 | font-size, font-weight, letter-spacing, color, text-decoration | skin: `.iw-tip-link` | — | ○ L293: A single stat block in a two-column grid strands its value in the middle of the card with an empty column beside it — the "Base value 551g" case, which is most trade goods and eve… |
| 46 | 386 | — | `.iw-tip-link:hover` | (0,2,0) | 1 / 0 | color | skin: `.iw-tip-link` | — | ○ L293: A single stat block in a two-column grid strands its value in the middle of the card with an empty column beside it — the "Base value 551g" case, which is most trade goods and eve… |
| 47 | 387 | — | `.iw-tip-link:focus-visible` | (0,2,0) | 3 / 0 | color, outline, outline-offset | skin: `.iw-tip-link` | — | ○ L293: A single stat block in a two-column grid strands its value in the middle of the card with an empty column beside it — the "Base value 551g" case, which is most trade goods and eve… |
| 48 | 392 | — | `.iw-tip-source` | (0,1,0) | 6 / 0 | margin-left, font-size, font-weight, letter-spacing, color, text-transform | skin: `.iw-tip-source` | — | ○ L293: A single stat block in a two-column grid strands its value in the middle of the card with an empty column beside it — the "Base value 551g" case, which is most trade goods and eve… |
| 49 | 400 | — | `.iw-tip-source.is-stale` | (0,2,0) | 1 / 0 | color | skin: `.iw-tip-source`<br>host: `.is-stale` | — | ○ L293: A single stat block in a two-column grid strands its value in the middle of the card with an empty column beside it — the "Base value 551g" case, which is most trade goods and eve… |
| 50 | 403 | `@media (max-width: 420px)` | `.iw-tip` | (0,1,0) | 5 / 0 | --fs-tip-w, --fs-tip-pad-x, --fs-tip-art, --fs-tip-corner-h, --fs-tip-corner-w | skin: `.iw-tip` | — | — |
| 51 | 410 | `@media (max-width: 420px)` | `.iw-tip-stats` | (0,1,0) | 1 / 0 | grid-template-columns | skin: `.iw-tip-stats` | — | — |
