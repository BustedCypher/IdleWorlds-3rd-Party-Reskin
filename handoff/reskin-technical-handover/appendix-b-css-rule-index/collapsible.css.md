# collapsible.css — rule index

> Generated by `tools/css-inventory.mjs` from `src/styles/collapsible.css` at commit `059081671bf3`. Do not edit by hand.

Injected as part of `<style data-iw-style="ui-system">` — stylesheet 7 of 7 in injection order, part 4 of 4 in that element's concatenation (src/modules/UIFoundation.js injectUIFoundationStyles()).

203 lines · 9,701 bytes · 15 rules · 15 selectors · 79 declarations · 75 `!important` · 2 `@media` · 0 `@container` · 0 `@keyframes` · 0 `@font-face`

**Columns.** *Line* is the rule's first line in the source file at the commit above. *Context* is the enclosing `@media`/`@container`/`@supports` chain. *Specificity* is the highest (id, class, type) of the rule's selector list, computed per Selectors Level 4 (`:is()/:not()/:has()` take their most specific argument, `:where()` counts zero). *Decl* is the declaration count; *!imp* the number marked `!important`. *Properties* lists every property the rule sets, with `!` marking `!important`. *Hooks* separates skin-owned hooks (`data-iw-*`, `data-fs-*`, `.fs-*`, `.iw-*`, `#iw-tip`) from game/host hooks (classes, attributes, elements the extension does not own). *Fragility* flags selectors that depend on utility class names, attribute substrings, element position or sibling order. *Comment* is the nearest authored comment for the rule's block (its own when marked ●, otherwise the last one above it in the same block, marked ○).

## Rules

| # | Line | Context | Selector(s) | Specificity | Decl / !imp | Properties | Hooks | Fragility | Comment |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | 22 | — | `[data-iw-collapse-head]` | (0,1,0) | 2 / 2 | position!, padding-right! | skin: `[data-iw-collapse-head]` | — | ● L16: `relative` makes this row the toggle's containing block, which is the whole * point: heading rows here measure anywhere from 30px to 93px tall depending on * the frame family and … |
| 2 | 24 | — | `[data-iw-collapse]` | (0,1,0) | 20 / 20 | position!, top!, right!, transform!, z-index!, display!, place-items!, width!, height!, padding!, margin!, border!, border-radius!, background!, box-shadow!, color!, cursor!, line-height!, -webkit-appearance!, appearance! | skin: `[data-iw-collapse]` | — | ○ L16: `relative` makes this row the toggle's containing block, which is the whole * point: heading rows here measure anywhere from 30px to 93px tall depending on * the frame family and … |
| 3 | 49 | — | `[data-iw-collapse]::after` | (0,1,1) | 7 / 7 | content!, width!, height!, border-right!, border-bottom!, transform!, transition! | skin: `[data-iw-collapse]` | — | ● L47: A rotated border corner, the same chevron the skill pager draws — no glyph, * so it needs no font and cannot be picked up by a text-matching classifier. |
| 4 | 61 | — | `[data-iw-collapse][aria-expanded="false"]::after` | (0,2,1) | 1 / 1 | transform! | skin: `[data-iw-collapse]`<br>host: `[aria-expanded="false"]` | — | ● L55: Expanded points UP — "fold this away" — and collapsed points DOWN, the * accordion convention: the chevron shows what the click will do, not what * the panel is already doing. |
| 5 | 72 | `@media (pointer: coarse)` | `[data-iw-collapse]::before` | (0,1,1) | 3 / 3 | content!, position!, inset! | skin: `[data-iw-collapse]` | — | — |
| 6 | 79 | — | `[data-iw-collapse]:hover` | (0,2,0) | 2 / 2 | border-color!, box-shadow! | skin: `[data-iw-collapse]` | — | ○ L55: Expanded points UP — "fold this away" — and collapsed points DOWN, the * accordion convention: the chevron shows what the click will do, not what * the panel is already doing. |
| 7 | 83 | — | `[data-iw-collapse]:focus-visible` | (0,2,0) | 2 / 2 | outline!, outline-offset! | skin: `[data-iw-collapse]` | — | ○ L55: Expanded points UP — "fold this away" — and collapsed points DOWN, the * accordion convention: the chevron shows what the click will do, not what * the panel is already doing. |
| 8 | 91 | — | `[data-iw-collapsed="1"] &gt; *:not([data-iw-collapse]):not([data-iw-collapse-head])` | (0,3,0) | 1 / 1 | display! | skin: `[data-iw-collapsed="1"]` `[data-iw-collapse]` `[data-iw-collapse-head]` | — | ● L88: The fold itself: every direct child goes except the heading row and the * control. |
| 9 | 113 | — | `[data-iw-collapsed="1"]` | (0,1,0) | 4 / 0 | --iw-frame-pad-y, --iw-frame-pad-mobile-y, --iw-frame-pad-x, --iw-frame-pad-mobile-x | skin: `[data-iw-collapsed="1"]` | — | ● L109: Re-point the TOKEN rather than fight the padding declaration. |
| 10 | 127 | — | `[data-iw-collapsed="1"] [data-iw-collapse-head]` | (0,2,0) | 6 / 6 | min-height!, margin!, padding-top!, padding-bottom!, padding-left!, gap! | skin: `[data-iw-collapsed="1"]` `[data-iw-collapse-head]` | — | ● L122: The surviving flourish reaches ~32px in from the frame's own left edge, * which is 10px past the frame's `--iw-frame-pad-x` — so the title needs its * own indent to sit clear of t… |
| 11 | 139 | — | `[data-iw-collapsed="1"] [data-iw-collapse-head] *:not([data-iw-collapse-spine]):not([data-iw-collapse])` | (0,4,0) | 1 / 1 | display! | skin: `[data-iw-collapsed="1"]` `[data-iw-collapse-head]` `[data-iw-collapse-spine]` `[data-iw-collapse]` | — | ● L135: Keep the path to the title and the control; drop the rest of the row. |
| 12 | 142 | — | `[data-iw-collapsed="1"] [data-iw-collapse-spine]` | (0,2,0) | 8 / 8 | display!, min-width!, min-height!, margin!, padding!, border!, background!, box-shadow! | skin: `[data-iw-collapsed="1"]` `[data-iw-collapse-spine]` | — | ○ L135: Keep the path to the title and the control; drop the rest of the row. |
| 13 | 154 | — | `[data-iw-collapsed="1"] [data-iw-collapse-title]` | (0,2,0) | 12 / 12 | font-family!, font-size!, font-weight!, line-height!, letter-spacing!, text-transform!, text-align!, color!, text-shadow!, white-space!, overflow!, text-overflow! | skin: `[data-iw-collapsed="1"]` `[data-iw-collapse-title]` | — | ● L152: The one treatment. `line-height` is a fixed px, not a ratio, because it is what makes every collapsed bar exactly as tall as every other. |
| 14 | 182 | — | `[data-iw-collapsed="1"]::after` | (0,1,1) | 9 / 9 | content!, position!, inset!, z-index!, pointer-events!, border-style!, border-width!, border-color!, border-image! | skin: `[data-iw-collapsed="1"]` | — | ● L169: One flourish, top left. |
| 15 | 201 | `@media (prefers-reduced-motion: reduce)` | `[data-iw-collapse]::after` | (0,1,1) | 1 / 1 | transition! | skin: `[data-iw-collapse]` | — | — |
